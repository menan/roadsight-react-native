import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    reportItem: {
        borderBottomWidth: 1,
        borderBottomColor: 'rgb(167, 165, 165)',
    }
  });